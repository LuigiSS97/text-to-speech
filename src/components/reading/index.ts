import { ReadingControls } from "./ReadingControls";
import { ReadingCurrently } from "./ReadingCurrently";
import { ReadingRoot } from "./ReadingRoot";
import { ReadingSentences } from "./ReadingSentences";

const Reading = {
  controls: ReadingControls,
  currentRead: ReadingCurrently,
  sentences: ReadingSentences,
  root: ReadingRoot,
};
export default Reading;
